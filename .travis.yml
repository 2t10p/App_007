language: node_js
node_js:
    - '8'

sudo: required
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
    - node_modules

install:
  - cd front-site
  - npm install

before_script:

script:
 - npm run unit

after_script:

branches:
  only:
    - master

notifications:
  email: false


