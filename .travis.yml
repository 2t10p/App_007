dist: trusty
sudo: false
addons:
  chrome: stable

language: node_js
node_js:
    - '8'

cache:
  directories:
    - node_modules

before_install:

install:
  - cd front-site
  - npm install

before_script:

script:

after_script:

branches:
  only:
    - master

notifications:
  email:
    recipients:
      - wico0628@gmail.com
    on_success: change
    on_failure: always


