language: node_js
node_js:
    - '8'

cache:
  directories:
    - node_modules

install:
  - npm install

before_script:

script:
  - npm run build

after_script:

branches:
  only:
    - master

env:
 global:

